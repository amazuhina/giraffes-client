import React from 'react'
import {Spinner} from "react-bootstrap";


export const Loading = ({}) => {
    return (
        <div >
            loading
            {/*<Spinner style={{width: '50px', height: '50px'}} animation="border" variant="success" size="sm" />*/}
        </div>
    )
}